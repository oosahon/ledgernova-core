import ledgerAccountCategoryMappingRule from './account-category-mapping.rule';

const accountingRules = Object.freeze({
  getApplicationCategories: ledgerAccountCategoryMappingRule,
});

export default accountingRules;
