# 5. Building Block View

The building block view shows the static decomposition of the system into building blocks (modules, components, subsystems) as well as their dependencies.

## 5.1 Level 1 (Whitebox: Overall System)

Description of the top-level architectural layers (e.g., app, domain, infra, interface, shared).

## 5.2 Level 2 (Whitebox: Domain Layer)

Deep dive into specific domain modules (e.g., ledger-account, category, transaction).
